pymupdf==1.24.11
reportlab==4.2.5
rapidfuzz==3.10.1
pydantic==2.9.2
pandas==2.2.3
pillow==10.4.0
pytest==8.3.3

# Required to process scans/photos/images (the [2B] VLM_EXTRACTION FSM
# state): set AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT and
# AZURE_DOCUMENT_INTELLIGENCE_KEY -- see src/providers/document_intelligence_provider.py.
# A digital PDF with a real embedded text layer (extraction/layout_ocr.py)
# still works with none of this configured; only image-format documents
# require it, and raise a clear error (AzureNotConfiguredError) if it's missing.
azure-ai-documentintelligence==1.0.2

# Optional: only needed for the HTTP interface (src/main.py). The CLI above
# is the primary entry point and needs none of this -- see README.
fastapi==0.115.0
uvicorn==0.31.0
python-multipart==0.0.12
python-dotenv==1.0.1
