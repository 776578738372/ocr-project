pymupdf==1.24.11
reportlab==4.2.5
rapidfuzz==3.10.1
pydantic==2.9.2
pandas==2.2.3
pillow==10.4.0
pytest==8.3.3

# Optional: only needed if you activate a real VLM extractor with a live API key.
# The pipeline runs end-to-end without either installed (MockVLMExtractor is the default).
anthropic==0.39.0
openai==3.17.0

# Optional: only needed for the HTTP interface (src/main.py). The CLI above
# is the primary entry point and needs none of this -- see README.
fastapi==0.115.0
uvicorn==0.31.0
python-multipart==0.0.12
python-dotenv==1.0.1
